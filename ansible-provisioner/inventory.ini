# ## Configure 'ip' variable to bind kubernetes services on a
# ## different ip than the default iface
# ## We should set etcd_member_name for etcd cluster. The node that is not a etcd member do not need to set the value, or can set the empty string value.


[all]
localserver ansible_host=192.168.1.189 ansible_ssh_user=vennpham ansible_ssh_private_key_file=~/.ssh/local
cloudtools ansible_host=192.168.122.26 ansible_ssh_user=venn  ansible_ssh_private_key_file=~/.ssh/local
postgres ansible_host=192.168.122.30 ansible_ssh_user=venn  ansible_ssh_private_key_file=~/.ssh/local
pi ansible_host=192.168.1.223 ansible_ssh_user=pi ansible_ssh_private_key_file=~/.ssh/id_ed25519

[vms]
haproxy
docker-server
elasticsearch01
kafka-1
kafka-2
kafka-3
postgres
cloudtools
pi

[cloudtools]
cloudtools

[etcd]
haproxy

[haproxy]
haproxy

# [k8s_cluster:children] — uncomment and define kube_control_plane/kube_node when k8s nodes are provisioned
# kube_control_plane
# kube_node

[postgres]
postgres

[docker]
n8n-server

[common]
common

[localserver]
localserver

[kafka_cluster]
kafka-1
kafka-2
kafka-3

[elasticsearch]
elasticsearch01
elasticsearch02
elasticsearch03


[talos]
localserver